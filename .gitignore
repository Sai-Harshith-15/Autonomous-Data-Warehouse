# === Worktrees (per §12.5 — isolated per-task checkouts) ===
work/

# === Harness DB — rebuildable projection (§8.3) ===
harness.db
harness.db-journal
harness.db-wal
harness.db-shm

# === Backups (§24.2) ===
backups/

# === Secrets (§13.4 — DPAPI store, never committed) ===
.secrets/

# === Artifacts — evidence store, too large for git (§14.2) ===
artifacts/

# === Events — JSONL logs, source of truth but too verbose for git ===
events/

# === Checkpoints — resume snapshots (§24.4) ===
checkpoints/

# === Approvals — pending human gates (§13.5) ===
approvals/pending/

# === OS noise ===
Thumbs.db
.DS_Store
desktop.ini

# === Hermes desktop attachments ===
.hermes/desktop-attachments/

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt

# === Node ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
*~

# === Test coverage ===
coverage/
*.coverage
.coverage.*
htmlcov/
.pytest_cache/

# === Harness CLI local state ===
.harness-local/
